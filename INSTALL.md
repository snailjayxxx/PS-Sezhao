# 安装与使用说明

## 推荐：安装 `.ccx`

适合普通使用者，也是本项目建议采用的发布方式。

### macOS / Windows

1. 安装或更新 Adobe Creative Cloud Desktop。
2. 安装 Photoshop 23.3 或更高版本。
3. 在 GitHub Releases 下载最新的 `PS-Sezhao-v0.1.1.ccx`。
4. 双击 `.ccx` 文件。
5. Creative Cloud 提示插件不是来自 Adobe Marketplace 时，确认文件来自本仓库，再选择“本地安装”或“安装”。
6. 完全退出并重新打开 Photoshop。
7. 打开 `插件（Plugins） → 胶片去色罩`。

升级旧版本时，可以直接安装新版 `.ccx` 覆盖；若 Creative Cloud 提示冲突，先卸载旧版再安装新版。

## 正确使用顺序

1. 打开负片扫描图或翻拍图，并选中负片所在像素图层。
2. 点击“自动分析边框”。
3. 当画面没有干净的橙色胶片边框时，先用矩形选框工具框选一块未曝光胶片，再点击“从当前选区采样”。
4. 底部状态显示“已完成基底分析，可以生成正片”后，调整胶片起始配置和参数。
5. 点击“生成正片图层”。

面板中部可以使用鼠标滚轮或触控板上下滚动。生成按钮、错误状态和进度条固定显示在面板底部。

## 为什么选择安装式，而不是独立程序

PS-Sezhao 需要读取 Photoshop 当前图层并把结果写回新图层。使用 `.ccx` 安装后，工作流最短、可保留 PSD 图层，也不需要用户安装 Python、Node.js 或其他运行环境。

独立程序更适合未来的文件夹批量转换，但会失去 Photoshop 选区、图层和即时修图衔接，因此不作为当前主方案。

## 开发者加载方式

当 `.ccx` 安装失败或需要调试时：

1. 在 Creative Cloud Desktop 安装 UXP Developer Tool。
2. 打开 Photoshop。
3. 打开 UXP Developer Tool。
4. 点击 `Add Plugin`，选择本仓库 `plugin/manifest.json`。
5. 点击 `Load`。
6. 回到 Photoshop，在插件菜单打开面板。

## 常见问题

### 点击“生成正片图层”提示需要先分析

这是正常的操作顺序保护。必须先点击“自动分析边框”，或者使用当前选区采样胶片基底。

### 自动分析提示边框像素不足

用矩形选框工具框选一块未曝光、没有齿孔和文字的橙色胶片边缘，然后点击“从当前选区采样”。

### 转正后偏色明显

检查是否采样到了画面、黑色片夹、齿孔或灯板直射区域。重新选择更干净的胶片基底，并确认输入图像没有发生颜色通道剪切。

### 面板不能滚动或旧按钮仍不显示

确认面板标题显示版本 `0.1.1` 或更高。若仍显示 `0.1.0`，说明 Photoshop 还在加载旧插件；请卸载旧版、安装新版，并完全重启 Photoshop。

### `.ccx` 被提示未验证

通过 GitHub 直接分发的插件没有经过 Adobe Marketplace 审核，Creative Cloud 会显示信任提示。请确认下载来源和 `CHECKSUMS.txt` 后再安装。
