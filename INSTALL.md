# PS-Sezhao 安装与使用说明

## 安装 v0.2.1

1. 安装或更新 Adobe Creative Cloud Desktop。
2. 安装 Photoshop 23.3 或更高版本。
3. 从 Releases 下载 `PS-Sezhao-v0.2.1.ccx`。
4. 完全退出 Photoshop。
5. 双击 `.ccx`，确认来源后选择“本地安装”或“安装”。
6. 若提示旧版本冲突，先在 Creative Cloud 中卸载旧版，再安装 v0.2.1。
7. 重新打开 Photoshop，从 `插件（Plugins） → 胶片去色罩` 打开面板。
8. 确认顶部显示 `PS-SEZHAO · 0.2.1`。

## 标准使用流程

1. 选中原始负片所在的像素图层。
2. 点击“自动分析边框”。
3. 若边框包含大量齿孔、片夹或灯板直射区域，框选一块干净的未曝光橙色胶片，再点击“从选区采样色罩”。
4. 分析完成后，插件创建名为 `PS-Sezhao · 实时预览（临时）` 的图层。
5. 直接拖动滑块；画布会在约 70ms 防抖后连续同步，松开滑块会立即刷新最终位置。
6. 满意后点击“生成最终正片图层”。最终图层使用原始文件的完整分辨率和位深。

## 实时预览说明

v0.2.1 会读取并保持当前文档的实际位深：

- 8 位文档使用 8 位预览缓冲区
- 16 位文档使用 16 位完整范围预览缓冲区
- 32 位文档使用浮点预览缓冲区

预览 ImageData 会继承原始图像或 Photoshop 文档的 ICC 配置，解决 v0.2.0 中以下错误：

```text
The target depth does not match the source data.
A color profile must be specified for the source data.
```

预览精度选择：

- **快速 800 px**：拖动最流畅，推荐用于集中调色。
- **标准 1200 px**：清晰度与速度较均衡。
- **精细 1800 px**：更适合检查细节，但16位大图刷新会更慢。

## 纠正严重偏冷照片

建议依次调整：

1. 增大“色温”。
2. 偏青或偏绿时，将“色调”往洋红方向增加。
3. 增加红色增益，或降低蓝色增益。
4. 若整卷都偏色，再微调胶片基底 R/G/B。
5. 可框选白衬衫、灰卡或应为中性的墙面，点击“从当前选区校正中性色”。

## 本卷参数

“保存为本卷参数”会保存：

- 胶片基底和密度范围
- 起始风格及强度
- 曝光、反差、明暗参数
- 色温、色调、RGB 增益
- 胶片基底三通道微调

对同卷下一张照片，选中原始负片层后点击“载入本卷参数”。

## 常见问题

### 拖动时仍然有轻微延迟

这是 UXP 插件对临时像素图层重新计算和写入所需的时间，不是 Photoshop 内置 GPU 调整层。先切换到 800px 预览，可以显著提高响应速度。

### 实时预览看起来不够精细

预览是为了快速交互而生成的低分辨率临时层。最终生成按钮会从原始负片重新进行全分辨率运算。

### 中性色校正提示选区太小

使用矩形或套索工具框选更大的白色、灰色区域。不要选择已经过曝、纯黑或具有明显固有颜色的物体。

### 自动分析结果偏色

重新框选干净的未曝光胶片基底进行采样，或使用基底 R/G/B 微调。齿孔、文字、黑色片夹和灯板直射区域不适合作为色罩样本。

### 仍显示旧版界面

完全退出 Photoshop，卸载旧插件后重新安装 `.ccx`。确认面板顶部版本号为 0.2.1。

## 开发者加载

可通过 UXP Developer Tool 添加 `plugin/manifest.json` 并点击 Load。调试时使用 `npm run check` 运行语法和回归测试。
