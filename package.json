{
  "name": "ps-sezhao",
  "version": "0.2.0",
  "private": true,
  "description": "Photoshop UXP plugin for local color-negative conversion, advanced color correction and live canvas preview.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run validate && npm test",
    "package:release": "bash scripts/build-release.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
