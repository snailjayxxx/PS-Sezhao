{
  "name": "ps-sezhao",
  "version": "0.3.3",
  "private": true,
  "description": "Unified Photoshop, Lightroom Classic and standalone color-negative conversion project.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run validate && npm test",
    "package:release": "bash scripts/build-release.sh"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
