{
  "name": "ps-sezhao",
  "version": "0.2.2",
  "private": true,
  "description": "Photoshop UXP plugin for color-negative conversion with click eyedroppers, large preview and live canvas correction.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run validate && npm test",
    "package:release": "bash scripts/build-release.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
