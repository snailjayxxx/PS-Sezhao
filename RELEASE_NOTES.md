# PS-Sezhao v0.2.2

本版本重点改善吸管交互和预览尺寸。

## 点击式吸管

- 新增“吸管：点击胶片基底”。启动后可直接点击 Photoshop 画布中的未曝光橙色胶片。
- 新增“吸管：点击中性色”。启动后可直接点击白衬衫、灰卡、灰墙等中性区域。
- 同一个吸管模式也支持直接点击插件内大图预览，作为 Photoshop 工具切换失败时的可靠备用方式。
- 支持单点、3×3、5×5、11×11、21×21 五种取样范围。
- 默认使用 11×11 中位数取样，降低胶片颗粒、灰尘和扫描噪点影响。
- 点击色罩后会重新计算密度黑白点并立即刷新预览。
- 点击中性色后会自动更新 RGB 输出增益。

## 大图预览

- 新增插件内大图预览，默认预览长边提升到 1800 px。
- 支持 1200、1800、2400、3200 px 四档预览精度。
- 支持适应窗口、100%、200% 显示比例。
- 支持“放大预览区”，高度可从 380 px 扩展到 680 px。
- 100% 和 200% 模式可在预览区域内滚动查看细节。
- 大图预览与 Photoshop 画布预览使用同一次处理结果，滑块变化后同步更新。

## 兼容性

- 最低 Photoshop 版本调整为 24.0，因为画布点击吸管使用 ColorSampler DOM。
- 最终正片仍从原始负片图层按完整分辨率、文档位深和 ICC 配置重新计算。

## 下载

普通用户安装 `PS-Sezhao-v0.2.2.ccx`。升级后请完全退出并重新打开 Photoshop。
