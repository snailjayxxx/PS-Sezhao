# PS-Sezhao v0.5.2

本版本修正胶片基底取样逻辑，并重新设计独立版和 Lightroom 高精度窗口的裁切交互。

## 胶片基底吸管始终读取原图

- 独立版即使正在显示转正、调色或裁切后的画面，胶片基底吸管也只读取未修改的原始输入像素；
- 裁切后的可见坐标会映射回完整原图，再从原图取样；
- 吸管取得基底颜色后，黑白范围在当前裁切区域内重新计算；
- Photoshop 吸管继续从记录的原始负片图层读取像素，不读取临时预览图层；
- 中性色吸管同样使用原图坐标，再结合当前转正参数计算 RGB 增益。

## 独立版新增胶片基底手动微调

Windows、macOS 独立版以及 Lightroom 高精度窗口新增“胶片基底手动微调”：

- 基底 R / G / B 三通道滑块；
- 数字输入框；
- `− / +` 单位微调；
- 输入框上下方向键微调；
- 显示原图识别值与当前实际使用值；
- 可一键重置三通道微调；
- 每张照片分别保存基底微调，也可以通过已有的参数同步功能应用到多张图片。

Photoshop 版原有“胶片基底微调”继续保留，并支持相同的数字输入和加减按钮。

## 新裁切方式

裁切从持续遮罩显示改为“编辑状态 / 应用状态”两阶段：

1. 平时只显示裁切后保留的画面，不显示裁切框，也不显示被裁掉的部分；
2. 点击“裁切”后恢复显示完整照片，并显示当前裁切框；
3. 可拖动四角、四边中点、框内区域，也可在框外重新拖出一个范围；
4. 点击“完成裁切”后重新只显示保留画面；
5. 再次点击“裁切”时，完整照片和上次裁切框会重新出现；
6. “重置裁切”恢复完整画面。

裁切仍然是非破坏性的，原始文件不会被修改。

## 裁切范围内自动分析边框

- “自动分析边框”只分析当前裁切范围；
- 完成或重置裁切后，会使用新的范围重新计算色调范围；
- 使用吸管得到的原图基底颜色会保留，只重新计算裁切区域内的黑白范围；
- 批量任务和 Lightroom 高精度任务在缺少已保存分析时，也会先裁切再自动分析。

## 继续保留的功能

- 相机 RAW 直读与16位线性 ProPhoto 解码；
- 多图列表、文件夹导入和批量输出；
- 每张图片独立参数；
- 缩放、平移和100%/200%显示；
- Photoshop 2024+ 插件；
- Lightroom 原生直接转正、高精度16位TIFF和快照恢复。

## 发行文件

- `PS-Sezhao-Photoshop-v0.5.2.ccx`
- `PS-Sezhao-Photoshop-Developer-v0.5.2.zip`
- `PS-Sezhao-LightroomClassic-macOS-arm64-v0.5.2.zip`
- `PS-Sezhao-LightroomClassic-Windows-x64-v0.5.2.zip`
- `PS-Sezhao-Standalone-macOS-arm64-v0.5.2.zip`
- `PS-Sezhao-Standalone-Windows-x64-v0.5.2.zip`

自动测试覆盖原图坐标映射、裁切控制点拖拽、裁切范围分析、批量任务分析顺序、Photoshop 原始图层取样结构，以及 macOS / Windows 打包。
