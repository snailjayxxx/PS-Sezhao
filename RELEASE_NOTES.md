# PS-Sezhao v0.1.2

修复 Photoshop 2026 中点击“自动分析边框”或“从当前选区采样”时出现的模态作用域错误 1000。

## 修复内容

- 将负片像素读取 `imaging.getPixels` 放入 `core.executeAsModal` 作用域。
- 将当前选区读取 `imaging.getSelection` 放入同一模态作用域。
- 分析完成前保持源文档、图层和像素缓存处于一致状态。
- 分析结束或异常时立即释放预览与选区图像数据。
- 为分析和生成操作增加 10 秒模态等待时间，减少与其他 Photoshop 操作冲突。
- 分析时底部状态改为“正在分析胶片基底”，不再错误显示“正在生成”。
- 生成时才显示“正在生成正片”。
- 增加模态分析和忙碌状态的自动回归测试。

## 使用顺序

1. 选中负片所在的像素图层。
2. 点击“自动分析边框”；没有干净边框时，框选未曝光胶片后点击“从当前选区采样”。
3. 等待底部显示“已完成基底分析，可以生成正片”。
4. 调整参数并点击“生成正片图层”。

## 下载

普通用户下载并安装 `PS-Sezhao-v0.1.2.ccx`。安装后请完全退出并重新打开 Photoshop，并确认面板顶部显示 `0.1.2`。
