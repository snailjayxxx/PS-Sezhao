# PS-Sezhao v0.6.1

本版本修复独立版右侧参数栏宽度固定的问题，并把图片列表、图片预览和参数调整区改为真正可自由拖动的三栏布局。

## 三栏宽度自由调整

独立版主窗口现在使用带明显拖动手柄的三栏分隔布局：

- 左侧：相片文件列表；
- 中间：图片预览与裁切；
- 右侧：输出与参数调整。

左右两条竖向分隔线均可直接用鼠标拖动。三栏都设置了合理的最小宽度，避免缩得过窄后按钮或图片区域无法使用。

## 右侧参数栏响应窗口宽度

旧版右侧滚动区把画布宽度固定为 `340 px`，内部参数内容固定为 `326 px`。因此即使窗口或右侧栏被拉宽，里面的风格选择器、滑块和说明文字仍保持窄宽度。

v0.6.1 改为：

- 右侧滚动画布宽度由当前栏宽决定；
- 内部参数容器实时跟随画布宽度；
- 下拉框、滑块、输出区域自动横向扩展；
- 说明文字的换行宽度随右栏一起变化；
- 垂直滚动、鼠标滚轮和拖放功能继续保留。

## 初始布局

首次打开时会根据窗口总宽度分配三栏：

- 文件列表保持适合查看文件名的宽度；
- 中间预览获得主要空间；
- 右侧参数区保持足够显示完整控件的宽度。

窗口打开后不会强制锁定比例，用户可继续自由拖动调整。

## 兼容性与验证

- 保留 v0.6.0 的扫描仪风格与胶卷风格库；
- 保留 RAW、裁切、旋转、拖放、撤销重做及批量导出；
- 重建三栏后会重新安装右侧滚轮、文件列表滚轮和外部拖放绑定；
- 新增自动测试，验证不同窗口宽度下三栏均满足最小可用宽度。

## 发行文件

- `PS-Sezhao-Photoshop-v0.6.1.ccx`
- `PS-Sezhao-Photoshop-Developer-v0.6.1.zip`
- `PS-Sezhao-LightroomClassic-macOS-arm64-v0.6.1.zip`
- `PS-Sezhao-LightroomClassic-Windows-x64-v0.6.1.zip`
- `PS-Sezhao-Standalone-macOS-arm64-v0.6.1.zip`
- `PS-Sezhao-Standalone-Windows-x64-v0.6.1.zip`
