# PS-Sezhao v0.2.0

这是第一版“可实时调色”的重大升级。

## 新增：画布实时预览

- 分析完成后自动建立 `PS-Sezhao · 实时预览（临时）` 图层。
- 拖动参数后约 180ms 自动刷新，不需要反复生成完整分辨率图层。
- 可选择 800、1200、1800 px 三档预览精度。
- 支持立即刷新、隐藏/显示和移除预览。
- 最终生成时仍从原始负片图层读取完整分辨率和原始位深，预览层不会参与最终运算。
- 预览先写入低分辨率临时像素层，再使用 Photoshop 图层缩放铺满画布。

## 新增：更高自由度校色

- 色温范围扩大到 -300～+300，可处理严重偏冷或偏暖的底片。
- 新增绿—洋红色调控制。
- 新增红、绿、蓝三个独立输出增益，范围 0.25～3.00。
- 新增胶片基底 R/G/B 独立微调，每通道可调 ±64。
- 新增风格强度 0～200%，可减弱或增强 Portra、Gold、Fuji、ECN-2 起始风格。
- 新增黑点、白点、阴影、高光控制。
- 新增“从当前选区校正中性色”：框选白衬衫、灰卡等中性区域即可自动计算 RGB 增益。

## 算法改进

- 色温和色调改为独立的乘性色彩适配，不再是首版幅度较弱的简单通道加减。
- 胶片配置矩阵、Gamma、反差、饱和度均可通过风格强度连续混合。
- 保留 8/16/32 位最终输出和 Alpha 通道。
- 本卷参数格式升级，可保存全部新增控制项。

## 测试

- 10 项图像引擎测试。
- 6 项 UXP 界面、模态调用和实时预览结构测试。
- JavaScript 语法、版本一致性和 CCX 打包结构检查。

## 下载

普通用户安装 `PS-Sezhao-v0.2.0.ccx`。升级后请完全退出并重新打开 Photoshop，确认面板顶部显示 `0.2.0`。
