# PS-Sezhao v0.2.1

这是针对 v0.2.0 画布预览故障的修复版，重点实现更接近 Photoshop 原生调整面板的“拖动滑块即更新画布”。

## 修复：位深与色彩配置不匹配

v0.2.0 固定以 8 位缓冲区生成预览。在 16 位文档中写入时，Photoshop 会提示：

```text
The target depth does not match the source data.
A color profile must be specified for the source data.
```

v0.2.1 改为：

- 预览读取使用当前文档原始位深：8 位、16 位或 32 位。
- 处理结果保持相同的 TypedArray 类型和 `componentSize`。
- 16 位数据同步使用完整范围设置。
- 创建预览 ImageData 时明确传入原图 ICC 配置；无法读取时使用文档配置，再回退到 sRGB。
- 中性色选区分析同样按实际位深计算，不再假定选区和图像都是 8 位。

## 改进：连续滑块预览

- 滑块 `input` 事件约 70ms 合并刷新，拖动过程中会持续更新 Photoshop 画布。
- 松开滑块时通过 `change` 事件立即同步最终位置。
- 如果上一次预览仍在计算，只保留最新一次参数，避免排队生成大量过期结果。
- 首次建立临时预览层后重复复用同一图层，不再每次删除、重建和重新缩放。
- 预览更新继续在 `executeAsModal` 的交互模式中执行。

## 保持不变

- 实时预览仍为低分辨率临时图层，用于快速判断色彩。
- “生成最终正片图层”仍从原始负片读取完整分辨率和原始位深。
- 原始负片图层不会被修改。

## 下载

普通用户安装 `PS-Sezhao-v0.2.1.ccx`。升级后完全退出并重新打开 Photoshop，确认面板顶部显示 `0.2.1`。
