# PS-Sezhao v0.7.0-beta.1

这是 v0.7 架构与整卷工作流的首个公开测试版本。正式稳定版 v0.6.3 继续保留；本版本用于验证新的项目管理、后台处理、几何校正、完整输出和跨电脑迁移能力。

## 启动与架构整理

- 主启动入口压缩为 7 个稳定组装阶段；
- 引擎、旧界面兼容层、处理服务、项目存储和拖放运行时分组安装；
- 初始化、界面创建、状态写入、图片切换、项目保存/恢复和导出事件统一经过生命周期收口层；
- 保留 macOS 拖放加载失败时的普通窗口安全降级；
- Windows 与 macOS 构建后均创建真实图形窗口进行验证。

## 整卷项目

- 支持多个胶卷项目及独立的图片顺序、当前图片和每张图片参数；
- 自动保存并恢复调色、胶片基底、RAW 设置、裁切、旋转、拉直、翻转、透视和输出设置；
- 支持项目级胶卷名称、胶卷型号、相机、日期、备注和画面编号；
- 显示已分析、已编辑和已导出进度；
- 支持输出预设保存并应用到当前或选中图片。

## 预览与后台处理

- 建立轻量缩略图、高质量编辑代理和完整分辨率输出三级读取；
- 编辑代理限制条目数和内存占用，并自动准备下一张图片；
- 保存当前、导出选中和导出全部使用可取消的后台队列；
- 单张失败不会中断整批任务；
- 输出文件先写临时文件，成功后原子替换。

## 几何与批量同步

- 支持自动范围可信度、拉直、水平翻转、垂直翻转和四角透视；
- 预览、独立版导出和 Lightroom 高精度任务使用相同几何顺序；
- 多图同步可分别选择基底、风格、明暗、颜色、RAW、几何、裁切和输出模块；
- 同步中途失败时整体回滚。

## 完整输出

- 支持 16 位 TIFF、8 位 TIFF、JPEG 和 PNG；
- 支持保留输入 ICC、ProPhoto RGB 和 sRGB；
- 支持最长边、固定宽度、固定高度、百分比和是否允许放大；
- 支持最终输出锐化、文件名模板和多种重名策略；
- 支持写入胶卷信息和生成接触印样。

## 归档与迁移

- 新增 `.psszproj` 胶卷项目归档；
- 可只保存项目参数，也可包含原图原始字节；
- 导入时创建独立项目，不覆盖现有项目和输出预设；
- 支持原图移动后的递归重新定位；
- 支持文件大小、SHA-256、画面编号和输出预设完整性检查；
- 支持 SQLite 在线备份和验证后恢复。

## 真实胶卷验收工具

源码包新增：

```text
scripts/validate-real-roll.py
```

该工具不会修改原图，可针对一卷 TIFF、JPEG、PNG 或相机 RAW 输出：

- 缩略图、编辑代理和完整解码耗时；
- 图像尺寸、类型和数值有效性；
- 胶片基底分析可信度；
- 转正结果动态范围与平均亮度警告；
- 逐张负片/正片对照审阅图；
- JSON 与 Markdown 验收报告。

自动构建使用可控测试图验证工具本身；真实相机和真实胶片结果仍需通过本 Beta 版本继续收集。

## 测试重点

请重点验证：

- 一卷 36 张以上 RAW 的导入、切换和内存占用；
- 中文、日文及特殊字符路径；
- NAS、移动硬盘和原图移动后的恢复；
- 批量导出取消、失败隔离和重名处理；
- 关闭程序后项目与参数恢复；
- macOS 与 Windows 之间的项目归档迁移；
- 预览与最终输出的一致性。

## 发行文件

- `PS-Sezhao-Photoshop-v0.7.0-beta.1.ccx`
- `PS-Sezhao-Photoshop-Developer-v0.7.0-beta.1.zip`
- `PS-Sezhao-LightroomClassic-Source-v0.7.0-beta.1.zip`
- `PS-Sezhao-LightroomClassic-macOS-arm64-v0.7.0-beta.1.zip`
- `PS-Sezhao-LightroomClassic-Windows-x64-v0.7.0-beta.1.zip`
- `PS-Sezhao-Standalone-macOS-arm64-v0.7.0-beta.1.zip`
- `PS-Sezhao-Standalone-Windows-x64-v0.7.0-beta.1.zip`
- `CHECKSUMS.txt`
