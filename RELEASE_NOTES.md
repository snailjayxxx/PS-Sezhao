# PS-Sezhao v0.1.1

修复首版面板在 Photoshop 中无法完整操作的问题。

## 修复内容

- 将 UXP 不支持的 CSS Grid 按钮布局改为 Flex，恢复“自动分析边框”和“从当前选区采样”按钮显示。
- 面板改为固定高度的内部滚动区，可滚动查看全部设置和说明。
- 将生成按钮、状态信息和进度条放到固定操作区，不再被面板高度裁掉。
- 修复面板显示时 DOM 尚未准备完成可能导致事件监听没有绑定的问题。
- 未分析胶片基底就点击生成时，现在会显示明确弹窗，不再表现为“无反应”。
- 生成期间按钮会显示“正在生成，请稍候…”，并持续显示进度状态。
- 修正选区采样缩略图边界的使用方式。
- 加强像素缓存释放与取消操作处理。

## 使用顺序

1. 选中负片所在的像素图层。
2. 点击“自动分析边框”；没有干净边框时，框选一块未曝光胶片后点击“从当前选区采样”。
3. 确认底部显示“已完成基底分析，可以生成正片”。
4. 点击“生成正片图层”。

## 下载

普通用户下载并安装 `PS-Sezhao-v0.1.1.ccx`。安装后请完全退出并重新打开 Photoshop。
